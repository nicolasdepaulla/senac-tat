//aula prof ramos redes,ult


o que acontece quando o router não conhece o caminho?
R- tudo o que o router não conhece ele envia para o gateway

o que é proxy?
R- O proxy tem a função de conectar o computador local à internet.

quais são as funções do proxy?
R- ele recebe e repassa todas as suas requisições ao site que você está acessando.

o que é firewall?
R- ele limita o acesso as portas do computador,impedindo qualquer imbecil/invasores na rede 

portas 80,110,25,21,23
       (80=trafego web);
       (110,recebimento de emails);
       (25,transporta mensagens entre servidores);
       (21,para controle de conexão);
       (23, usado para telnet,considerado inseguro);

o que é dns?
R- traduz nomes de pessoas para endereço ip,para identificar e localizar recursos de redes




int 13 = vermelho;
void loop(){
    digitalWrite(vermelho,HIGH);
    digitalWrite(verde,LOW);
    digitalWrite(amarelo,LOW);
    if(vermelho <= 250;)
}

